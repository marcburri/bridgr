% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.bridge}
\alias{plot.bridge}
\title{Plot a Bridge Model}
\usage{
\method{plot}{bridge}(
  x,
  type = c("forecast", "fit"),
  level = 95,
  xlab = NULL,
  ylab = NULL,
  main = NULL,
  col_history = "black",
  col_fit = "#1b9e77",
  col_forecast = "#d95f02",
  col_interval = grDevices::adjustcolor("#d95f02", alpha.f = 0.2),
  lwd = 2,
  ...
)
}
\arguments{
\item{x}{A \code{"bridge"} object returned by \code{\link[=bridge]{bridge()}}.}

\item{type}{Plot type. Use \code{"forecast"} to plot the observed target history
together with the bridge forecast, or \code{"fit"} to compare the observed
target to the in-sample fitted values.}

\item{level}{Prediction interval level passed to \code{\link[forecast:reexports]{forecast::forecast()}} when
\code{type = "forecast"}.}

\item{xlab, ylab, main}{Optional axis and title labels. When omitted, sensible
defaults are chosen from \code{type}.}

\item{col_history, col_fit, col_forecast, col_interval}{Colors used for the
historical target, fitted values, forecast mean, and forecast interval.}

\item{lwd}{Line width for the main series.}

\item{...}{Additional graphical parameters passed to \code{\link[graphics:plot.default]{graphics::plot()}}.}
}
\value{
\code{x}, invisibly.
}
\description{
Visualize a fitted \code{\link[=bridge]{bridge()}} model either as an in-sample fit or as a
forecast with prediction intervals.
}
