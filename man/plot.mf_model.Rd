% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.mf_model}
\alias{plot.mf_model}
\title{Plot a Mixed-Frequency Model}
\usage{
\method{plot}{mf_model}(
  x,
  type = c("forecast", "fit"),
  level = 95,
  history_n = 50,
  xlab = NULL,
  ylab = NULL,
  main = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{"mf_model"} object returned by \code{\link[=mf_model]{mf_model()}}.}

\item{type}{Plot type. Use \code{"forecast"} to plot the observed target history
together with the bridge forecast, or \code{"fit"} to compare the observed
target to the in-sample fitted values.}

\item{level}{Forecast interval level passed to \code{\link[=forecast.mf_model]{forecast.mf_model()}} when
\code{type = "forecast"}.}

\item{history_n}{Number of historical target observations to display.
Defaults to the most recent \code{50}. Set to \code{NULL} to show the full history.}

\item{xlab, ylab, main}{Optional axis and title labels. When omitted, sensible
defaults are chosen from \code{type}.}

\item{...}{Additional arguments passed to \code{\link[=theme_bridgr]{theme_bridgr()}}.}
}
\value{
A ggplot2 object.
}
\description{
Visualize a fitted \code{\link[=mf_model]{mf_model()}} object either as an in-sample fit or as a
forecast with prediction intervals.
}
\examples{
gdp_growth <- tsbox::ts_pc(gdp)
gdp_growth <- tsbox::ts_na_omit(gdp_growth)
model <- mf_model(
  target = gdp_growth,
  indic = baro,
  indic_predict = "auto.arima",
  indic_aggregators = "mean",
  h = 1
)

plot(model, type = "fit")
}
